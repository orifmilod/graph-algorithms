#include "./Graph.cpp"
#include <iostream>
using namespace std;

// Get minimum number of colors to color the graph

// Get all the possible graph coloring

void backtrack(vector<vector<string>> &all, vector<string> current) {}

vector<vector<string>> possibleGraphColors(vector<string> &allColors,
                                           Graph graph) {

  vector<vector<string>> allPossibilities;
  vector<string> currentColors(graph.numberOfNodes(), "");
  backtrack(allPossibilities, currentColors);
  return allPossibilities
}
int main() {
  Graph graph;

  return 0;
}
